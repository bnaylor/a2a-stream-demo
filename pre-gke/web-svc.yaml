apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: a2a-demo
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
      # NodePort: browser reaches http://10.3.10.<node>:<nodePort>. Pin later.
  type: NodePort
