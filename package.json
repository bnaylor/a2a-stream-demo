{
  "name": "a2a-stream-demo",
  "private": true,
  "type": "module",
  "workspaces": ["protocol", "agents/*", "web"],
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "typecheck:web": "tsc -p web --noEmit",
    "smoke": "tsx scripts/smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
