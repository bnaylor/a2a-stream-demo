apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: a2a-demo
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
      # Exposure is an overlay concern: NodePort 30080 on local, LoadBalancer
      # on gke.
  type: ClusterIP
